action: state_intent_to_buy(person p, hobby h, item i).
pre: hobby_of(p, h) ^ used_in_hobby(i, h) ^ !intent_to_buy(p, i).
post: intent_to_buy(p, i) ^ introduced(i).
text: "(p) wants to buy a new [i] for [h].".
alias: .

action: state_intent_to_buy(person p, profession prof, item i).
pre: profession_of(p, prof) ^ used_in_profession(i, prof) ^ !intent_to_buy(p, i).
post: intent_to_buy(p, i) ^ introduced(i).
text: "(p) wants to buy a new [i] for work.".
alias: .

action: state_intent_to_buy_many(person p, concrete c).
pre: introduced(p) ^ !intent_to_buy_many(c) ^ desired_many(p, c).
post: intent_to_buy_many(p, c).
text: "(p) wants to buy as many [c] as possible.".
alias: .

action: state_intent_to_travel(person p, traveling t, country c).
pre: hobby_of(p, t) ^ !been_in(p, c) ^ !intent_to_travel(p, c).
post: intent_to_travel(p, c) ^ introduced(c).
text: "(p) is planning to travel to [c] for a vacation.".
alias: .

action: state_intent_to_travel(person p, athlete a, country c).
pre: profession_of(p, a) ^ !been_in(p, c) ^ !intent_to_travel(p, c).
post: intent_to_travel(p, c) ^ introduced(c).
text: "(p) will be to travelling to [c] for a sports competition.".
alias: .

action: state_intent_to_convert(person p, country c, measure_system ms, height n, distance_unit src, distance_unit dest).
pre: property_of(p, n) ^ in_unit(n, src) ^ !known_in_unit(n, dest) ^ stated_measure_system(c) ^ measure_system_in(c, ms) ^ unit_of_measure_system(dest, ms) ^ intent_to_travel(p, c) ^ !intent_to_convert(n, src, dest).
post: intent_to_convert(n, src, dest).
text: "(p) should convert (n) to [dest].".
alias: .

action: state_intent_to_convert(person p, country c, measure_system ms, weight n, mass_unit src, mass_unit dest).
pre: property_of(p, n) ^ in_unit(n, src) ^ !known_in_unit(n, dest) ^ stated_measure_system(c) ^ measure_system_in(c, ms) ^ unit_of_measure_system(dest, ms) ^ intent_to_travel(p, c) ^ !intent_to_convert(n, src, dest).
post: intent_to_convert(n, src, dest).
text: "(p) should convert (n) to [dest].".
alias: .

action: state_intent_to_compute_area(person p, concrete c).
pre: introduced(p) ^ introduced(c) ^ !intent_to_compute_area(c).
post: intent_to_compute_area(c).
text: "(p) wants to compute the area of [c].".
alias: .

action: state_intent_to_compute_volume(person p, concrete c).
pre: introduced(p) ^ introduced(c) ^ !intent_to_compute_volume(c).
post: intent_to_compute_volume(c).
text: "(p) wants to compute the area of [c].".
alias: .

action: state_intent_to_compute_diameter(person p, concrete c).
pre: introduced(p) ^ introduced(c) ^ !intent_to_compute_diameter(c).
post: intent_to_compute_diameter(c).
text: "(p) wants to know the length of the radius of the [c].".
alias: .


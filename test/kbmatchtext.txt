#types;
pet;
dog->pet;
cat->pet;
person;
student->person;
nonnegativevalue*;
distancevalue*->nonnegativevalue*;
height*->distancevalue*;

#relationships;
has(person height*);
canown(person pet);
bestfriend(person dog);

#actions;
name: feed;
parameters: person pet;
preconditions: owns(person pet) hungry(pet);
effects: !hungry(pet);